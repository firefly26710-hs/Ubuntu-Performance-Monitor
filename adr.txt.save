2026-04-10 (DAY 19)
Context(背景) :




Decision(決策) :
我首先想到用DayOfWeek 但又發現還要知道確切哪一周的 因此引入currentWeek來做更詳細的索引 因為如果不用currentWeek那麼我們可能會把每個月的4個周一都提取出來xx

Consequence(結果) :  如果我們還想去以月為單位查詢 能夠再加個month去做更詳細的索引 然後雖然這app沒有讓你可以查上周的資料功能 但我們能透過currentWeek - 1去做實現
Context(背景) :
I stared to define what the Dao we need
This will affect the complexity of managing data transmission(資料傳輸)
This is the first thing should be done
Decision(決策) :
RecorderDao :
BudgetDao :
DashBoardDao :
Consequence(結果) :
